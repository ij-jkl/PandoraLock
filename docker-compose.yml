services:
  postgres:
    image: postgres:latest
    container_name: pandora_postgres
    restart: always
    env_file:
      - .env
    ports:
      - "${POSTGRES_PORT}:5435"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
